apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
commonLabels:
  app.kubernetes.io/name: pgopher
resources:
  - deploy.yml
configMapGenerator:
  - name: pgopher
    files:
      - config.yml
