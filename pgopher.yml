listenAddress: ":8000"
profilingTargets:
  - name: test
    url: http://localhost:8010/debug/pprof/profile
    duration: 5s
    schedule: "* * * * *"
sink:
  type: file
  fileSinkOptions:
    folder: profiles
